---
import Agenda from "./Agenda.astro";
import { renderToc } from "../../lib/render_toc";

interface Props {
	content: string;
}

const { content } = Astro.props;
---
<Agenda  headings={renderToc(content)}/>
